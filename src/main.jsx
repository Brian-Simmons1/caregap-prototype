import React from "react";
import ReactDOM from "react-dom/client";
import YosiPrototype from "./YosiPrototype.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <YosiPrototype />
  </React.StrictMode>
);
